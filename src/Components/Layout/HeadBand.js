import classes from "./HeadBand.module.css";

const HeadBand = (props) => {
  return (
    <div className={classes.HeadBand}>
      All our meals are cooked with high-quality
      ingredients, just-in-time and of course by experienced
      chefs!
    </div>
  );
};

export default HeadBand;
